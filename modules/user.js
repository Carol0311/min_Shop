/**
 * Created by Administrator on 2017/7/14.
 */
/**var mongoose = require('mongoose');
module.exports = mongoose.model('User',new mongoose.Schema({
    password:String,
    email:String,
    avatar:String
}));*///use schema to define the fields and use the model to communicate with server and return the object User with these fields